# ai-autogen-agent-patterns

Install the packages
```
pip install -r requirements.txt
```

Create the file `.env` and add the following setting:
```
AZURE_AI_SERVICE_NAME=your-azure-ai-service-name
```